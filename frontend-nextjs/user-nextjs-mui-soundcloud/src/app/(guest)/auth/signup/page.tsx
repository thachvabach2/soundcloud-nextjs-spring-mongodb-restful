import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Signup - SoundCloud",
}

const SignupPage = () => {
    return (
        <div>
            signup page
        </div>
    )
}

export default SignupPage;